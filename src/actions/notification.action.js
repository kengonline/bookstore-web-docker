export const OPEN_TIMEOUT = 'OPEN_TIMEOUT';
export const openTimeout = () => {
    return {
        type: OPEN_TIMEOUT
    }
}

export const CLOSE_TIMEOUT = 'CLOSE_TIMEOUT';
export const closeTimeout = () => {
    return {
        type: CLOSE_TIMEOUT
    }
}