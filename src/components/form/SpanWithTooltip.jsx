import styled from "styled-components";

const SpanWithTooltip = styled.span`
    & {
            vertical-align: middle;
            line-height: 40px;
            display: inline-block;
            overflow: hidden;
            white-space: nowrap;
    }
`

export default SpanWithTooltip;